# ── Runtime / instance data (jangan di-commit) ──────────────────────────────
instance/
# (accounts.db & siap_users.db ada di instance/)

# ── Output scraping & dataset besar ─────────────────────────────────────────
data/
*.json
*.zip

# ── Python ───────────────────────────────────────────────────────────────────
__pycache__/
*.pyc
*.pyo
.env

# ── Cache HuggingFace / model weights ────────────────────────────────────────
*.bin
*.safetensors

# ── File sementara / tidak relevan ───────────────────────────────────────────
claude\ --resume\ *.txt
test_*.json
